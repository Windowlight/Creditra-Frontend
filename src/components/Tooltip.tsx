/**
 * Tooltip — alias for `AccessibleTooltip`.
 *
 * This is a thin re-export of the accessible tooltip primitive so consumers
 * who search for "Tooltip" (or expect the shorter name) find it immediately.
 * All accessibility guarantees come from `AccessibleTooltip`.
 *
 * @see AccessibleTooltip
 */
export { AccessibleTooltip as Tooltip } from './AccessibleTooltip';
